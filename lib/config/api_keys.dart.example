// lib/config/api_keys.dart.example
// Copy this file to api_keys.dart and fill in your real keys, OR pass them
// at build time via --dart-define-from-file=env.json (see env.example.json).
// api_keys.dart and env.json are in .gitignore and must NEVER be committed.

class ApiKeys {
  // Gemini API key — https://aistudio.google.com/app/apikey
  static const String geminiApiKey = 'YOUR_GEMINI_API_KEY';
}
